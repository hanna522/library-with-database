extends layout 

block content 
  h1= title

  if genre_list.length 
    ul 
      each genre in genre_list
        if genre.books
        li 
          a(href = genre.url) #{genre.name}
  
  else 
    p There is no Genre.